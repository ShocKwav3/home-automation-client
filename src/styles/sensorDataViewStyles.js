import { makeStyles } from '@material-ui/core/styles'


export const sensorDataViewStyles = makeStyles(theme => ({
  container: {
    flexGrow: 1,
  },
  cardContainer: {
    //minHeight: 100,
    //minWidth: 250,
  },
}));